# Frontend environment variables (Vite).
# The React app reads the Turnstile site key from /auth/turnstile-sitekey at
# runtime, so no Cloudflare secrets are needed here.

# Vite dev server port (optional).
# VITE_PORT=5173
